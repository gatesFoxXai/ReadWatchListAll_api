# cStocks 產品需求文件（PRD）

## 1. 產品願景

**cStocks** 是一套以 Python + Matplotlib 為核心的台股技術分析與量價視覺化工具。目標使用者為具備量化思維的個人投資者與程式設計者，需在單一視窗內整合 K 線、成交量、MACD、KDJ、籌碼診斷（大戶／散戶）與手繪標註，並支援多週期切換與歷史區間平移，輔助「持有／減碼／觀望」等決策。
長期方向：與元大 OneAPI 即時行情、盤中預估量、設定持久化（JSON）及獨立繪圖模組整合。
Web_Dashboard.py — 即時多股監控畫面,待完善由agent掌控,預計重此處,規劃喚起 python cStocks.py -@code.csv

---

## 2. 目標使用者

| 角色 | 需求 |
|------|------|
| 量化愛好者 | 多週期、多指標、可平移歷史、支撐／壓力參考 |
| 程式設計者 | 以 CSV 驅動、類別清晰、可擴充指標與 UI |
| AI Agent | 依 `AGENT.md` / `SPEC.md`/SPEC.md 修改程式，不破壞既有功能 |

---

## 3. 核心使用情境

1. **分K/日內／波段檢視**：載入 `*.csv`，切換 1 分～月 K，觀察均線、布林、量能。2種api csv來源:1min\為極短線:code1分K.csv;@code.csv為短期日k
2. **籌碼解讀**：游標懸停顯示均價、每筆均量、大戶／散戶狀態（🐋／👥）。
3. **歷史比對**：左鍵拖曳平移 X 軸，觀察支撐／壓力與換手區。資料來源,歷史\csvd
4. **標註留存**：水平線、通道、斐波那契、測距等繪圖依股票代碼存成 `{code}_drawings.json`。
5. **多股對照**：`plot_all(block=False)` 同時開啟多視窗（如 2317、2330）。

---

## 4. 功能需求

### 4.1 資料與週期（P0）

| ID | 需求 | 狀態 |
|----|------|------|
| D-01 | 從 CSV 讀取 OHLCV、成交金額、成交筆數 | ✅ 日k-月k已完成,[代辦1分k~60分k條件式載入切換資料源共用類,擴充至60分K(60ma&240ma)] |
| D-02 | 日期欄位 `datetime`，排序後作為索引基礎 | ✅ 已完成 |
| D-03 | 週期重採樣：1/5/20/40/60/240 分、日、週、月、雙月、季、年 |  部分完成,配合分k重構 |
| D-04 | 切換週期時保留足夠歷史緩衝（`df_all`）供平移 | ✅ 已完成 |
| D-05 | 可視窗口長度 `n_days`（預設 60，可於 `load_data` 指定） | ✅ 已完成 |
| D-06 | 系統必須能主動發現自己正在「鬼打牆」。實作指標：建立一個計時器與 Hash 雜湊表。如果 同一個錯誤訊息連續出現 3 次以上，迭代的評估分數（Score）沒有任何提升，立刻觸發中斷sub-agent,並提示「鬼打牆」回報agent。


### 4.2 技術指標（P0）

| ID | 需求 | 狀態 |
|----|------|------|
| I-01 | MA5 / MA20 均線 | ✅ 已完成,[ 可調擴充至可選60分K(I-08 60ma&240ma)] |
| I-02 | 布林通道（20 期、2σ） | ✅ 已完成 |
| I-03 | MACD（12, 26, 9）含 DIF/DEA 柱狀、金叉標記 | ✅ 已完成 |
| I-04 | KDJ（9, 3, 3）含 80/20 參考線 | ✅ 已完成 |
| I-05 | 成交量 5 日均量線 | ✅ 已完成 |
| I-06 | 均價（成交金額／成交股數）、每筆均量及 5 日均量 | ✅ 已完成 |
| I-07 | 成交量加權支撐／壓力（`_calc_support_resistance`，最多各 2 條） | ✅ 已完成 |
| I-08 | 可調短均／長均（設定檔，最長 240ma） | ✅ 已完成 |
| I-09 | 20 日成交均量線 | ✅ 已完成 |
| I-10 | 盤中預估量（依台股 09:00–13:30 時間倍數） | ✅ 已完成（2026-05-26） |

### 4.3 圖表與 UI（P0–P1）

| ID | 需求 | 狀態 |
|----|------|------|
| U-01 | 四聯圖：價格、成交量、MACD、KDJ，共用 X 軸 | ✅ 已完成 |
| U-02 | 亮色／深色主題（`StockPalette`） | ✅ 已完成 |
| U-03 | K 線紅漲綠跌、刻度與極值價格標籤（`TICK_INTERVAL`） | ✅ 已完成 |
| U-04 | 游標十字線 + 資訊盒（OHLC、指標、量能張數） | ✅ 已完成 |
| U-05 | 左鍵拖曳水平平移歷史 | ✅ 已完成 |
| U-06 | 右側週期 Radio、繪圖工具與預設色 Radio | ✅ 已完成 |
| U-07 | 繪圖工具：水平／垂直線、切線、箭頭、通道、矩形、弧、斐波、測距、選取、清除 | ✅ 已完成 |
| U-08 | 繪圖依 `{code}_drawings.json` 持久化，含 `unit` 分週期 | ✅ 已完成 |
| U-09 | 匯出 PNG（150 dpi，📷 按鈕） | ✅ 已完成 |
| U-10 | 價格 Y 縮放（Shift+滾輪、Y↺ 還原） | ✅ 已完成 |
| U-11 | X 軸縮放（滾輪／＋－，不改週期） | ✅ 已完成 |
| U-12 | X 軸日期標籤自適應、減少與最後一筆重疊 | ✅ 已完成 |
| U-13 | 參數 JSON 持久化（`{code}_settings.json`） | [] 已部分完成（⚙ 按鈕；須追加完整 UI 面板） |
| U-14 | 自訂 RGBA 調色盤（均線、K 線等）寫入 JSON | [] 已部分完成（須追加完整 UI 面板，settings.json 持久化） |
| U-15 | 指定日期文字備註框（前景／背景色可調） |  [] 已部分完成（2026-05-26，[N] 備註工具,內文字定義,並綁定{unit}） |

### 4.4 籌碼與決策輔助（P1）

| ID | 需求 | 狀態 |
|----|------|------|
| C-01 | 大戶判定：每筆均量 > 5 日均量 × 1.2 | ✅ 已完成 |
| C-02 | 🐋↗／🐋↘／👥 與狀態文字（追價、吸收、脫手等） | ✅ 已完成 |
| C-03 | 量能 🔥／📊 與 5 日均量比較 | ✅ 已完成 |
| C-04 | 輸出明確買賣建議（需規則引擎或外部 Agent） | ⬜ 規劃中 |

---

## 5. 非功能需求

| 項目 | 說明 |
|------|------|
| 執行環境 | Python 3.13+，`requirements.txt` 所列套件 |
| 效能 | K 線向量化繪製；支撐壓力快取 `_sr_cache`，`update_view` 時重算 |
| 相容性 | Windows 中文字型；Emoji 經 **Pillow** 渲染（見 `requirements.txt`） |
| 可維護性 | `BasicUnit` 管週期與配色；繪圖邏輯待抽成獨立類別 |
| 原則 | **不得移除已完成功能**；改善前有不確定處先討論 |

---

## 6. 資料來源

- **現階段**：專案目錄內 `*.csv`（欄位見 `SPEC.md`），範例 `2317.csv`、`2330.csv`。持久記憶價量保存值API原始值不要/1000（如成交量,成交金額），顯示時除以 1000。
- **未來**：元大 OneAPI 即時五檔、每 5 秒 OHLCV 落檔（見 `claude_agent_setup.py` 系統提示）。

---

## 7. 成功指標

1. 單一命令 `python cStocks.py` 可開啟至少一支股票完整四聯圖。
2. 切換週期與拖曳平移後，指標與繪圖（同 `unit`）顯示正確。
3. 游標資訊與大戶狀態與手動計算樣本一致（誤差在顯示精度內）。
4. 重啟後 `{code}_drawings.json` 標註可還原至對應日期。

---

## 8. 路線圖（建議優先序）

1. **設定持久化**：`settings.json`（週期、n_days、TICK_INTERVAL、MA 週期、主題）。
2. **指標擴充**：20 日均量、可調 MA 週期至 240。
3. **互動**：垂直縮放邊界、X 軸標籤去重。
4. **配色**：`StockPalette` + 每 `BasicUnit` 週期獨立 JSON 色盤。
5. **架構**：`DrawingTool` 獨立類別；盤中預估量模組。
6. ** 自動啟動工具 ** : RUN.PY 實際API資訊可借各種公開資訊,驗證數據的正確性問題原因,以切割數據問題或顯示問題, SIM_RUN.PY ONLY FOR DEBUG 休市日模擬
7. **整合**：OneAPI 即時資料管線,分離2條線路sim_run與run。

---

## 9. 範圍外（Out of Scope）

- 券商下單、帳務管理
- 多使用者權限與雲端同步
- 非台股市場預設支援（可透過 CSV 擴充但非 PRD 焦點）

---

## 10. 修訂紀錄

| 版本 | 日期 | 說明 |
|------|------|------|
| 1.0 | 2026-05-24 | 依 README 與 `cStocks.py` 現況重寫 |
| 1.1 | 2026-05-24 | 對齊 `cStocks.py`（989 行）：修正 PNG／支撐壓力狀態 |
| 1.2 | 2026-05-24 | 程式：S/R 重繪、平移／繪圖分離、Pillow、genai 註解 |
| 1.3 | 2026-05-26 | 修復 6 項 BUG + 4 項新功能：X軸重疊、slice、選取換色、emoji方塊、載入圖選取、按鈕覆蓋、Ctrl+Y縮放、盤中預估量、面板: 備註工具、尚未實做自定義RGBA調色 |
| 1.4 | 2026-05-26 | 自動化工具 run.py 與 sim_run.py 交易日,非交易日模擬器替代來源資料,schedule 待完善由agent掌控
| 1.5 | 2026-05-26 | Web_Dashboard.py — 即時多股監控畫面,資料來源csv檔,定時刷新達成監控
